const Number = ({ children }) => {
    return <div
        className="text-2xl md:text-3xl xl:text-4xl font-semibold text-slate-700 tracking-wider"
    >
        {children}
    </div>
}

export default Number
